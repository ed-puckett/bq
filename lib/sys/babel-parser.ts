// @ts-ignore  // types not available for the imported module
import * as babel_parser from '@babel/parser/lib/index.js';

export const {
    parse,
    parseExpression,
    tokTypes,
} = babel_parser;
